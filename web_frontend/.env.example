# Admin + landing dev server API target
#
# Option A — use deployed Render API (recommended for /admin with real data):
VITE_API_BASE_URL=https://ai-legal-assistant-fes8.onrender.com
#
# Option B — local FastAPI (comment line above, start backend first):
# VITE_API_BASE_URL=http://127.0.0.1:8000
#
# Option C — omit this file entirely: Vite proxies /api → http://127.0.0.1:8000
#   cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000
